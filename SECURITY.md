# 安全策略

## 报告问题

远程仓库建立后，请优先使用 GitHub Private Vulnerability Reporting；不要在公开 Issue 中提交密钥、个人信息或可直接利用的漏洞细节。

## 基本原则

- 密钥只保存在托管平台或本地忽略文件中，不进入 Git 历史。
- 不在客户端代码中存放私有令牌。
- 外部脚本、字体、图片和统计服务必须经过来源与隐私评估。
- 依赖更新由自动提醒发现、人工审查；Vinext 等预发布依赖禁止自动合并。
- 如怀疑密钥泄漏，先撤销并轮换密钥，再清理历史和调查影响。

## 构建期图片边界

- 当前 Vinext 预发布版本会间接使用 `image-size` 解析图片；依赖审计报告的相关问题只影响构建环境，不进入线上运行依赖。
- 本站没有访客上传或远程图片解析接口。进入构建流程的媒体必须先由维护者确认来源，并限制为仓库内的 PNG、JPEG、WebP 或受控 SVG。
- 动画封面由同步脚本下载后统一通过 Sharp 转为本地 WebP；游戏封面只从本机 Steam 客户端缓存读取，并经过同样的格式与尺寸收敛。不得把未知来源的 ICNS、JXL、HEIF 等文件直接加入 `public/`。
- 在上游发布可用修复后，先验证 Worker SSR 与 GitHub Pages 静态导出，再升级 Vinext；不要为消除审计提示而盲目切换预发布版本。

## Steam 本地同步边界

- `npm run sync:games` 是维护者在本机主动运行的构建期工具，不会在浏览器、Worker、CI 或访客设备上访问 Steam。
- 脚本会在内存中读取 Steam 产品缓存和唯一一份本地用户配置，以“存在正向游玩记录”确定收藏范围；遇到多个本地用户缓存时直接停止，避免把不同账号混成公开目录。
- 写入仓库的字段仅限作品名、Steam App ID、平台、封面相对路径与公开商店 URL。不得写入账号标识、具体游玩时长、最近上线、好友资料、会话、令牌或其他授权信息，也不得从时长推导排名。
- 调试同步脚本时禁止输出完整 `localconfig.vdf` 或用户目录名；日志只允许报告条目数、封面数与产品缓存更新时间。怀疑本地授权数据进入日志或 Git 历史时，应立即停止发布并按密钥泄漏流程处理。
